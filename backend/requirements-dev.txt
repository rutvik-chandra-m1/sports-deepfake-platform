# Development/CI-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# ===== Testing =====
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2
# Reads generated PDFs back in tests to assert the limitations really render.
pypdf==6.1.1

# ===== Lint / Type-check =====
ruff==0.7.4
mypy==1.13.0
