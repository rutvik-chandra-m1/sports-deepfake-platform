# Architecture Decision Records

Decisions that are non-obvious, contested, or likely to be questioned — recorded with the
reasoning available at the time, so the argument does not have to be reconstructed later.

| # | Decision | Status |
|---|---|---|
| [0001](0001-vit-pytorch-instead-of-cnn-tensorflow.md) | Vision Transformer + PyTorch instead of the PPT's CNN + TensorFlow | Accepted |

Decisions recorded elsewhere because they belong with their measurements:

- **Dataset normalization and the confound audit** — `docs/dataset.md`
- **Learned fusion replacing hand-picked weights** — `docs/evaluation.md`
- **Provenance as a log-odds layer rather than a fitted feature** — `docs/models.md`
- **Security trade-offs, including what remains open** — `docs/security.md`
